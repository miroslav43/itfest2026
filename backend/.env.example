# Copy to .env and fill in your values
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
SECRET_KEY=replace_with_a_long_random_secret_key
# Comma-separated. Add capacitor://localhost and https://localhost for the mobile APK.
ALLOWED_ORIGINS=http://localhost:3000,https://localhost,http://localhost,capacitor://localhost
